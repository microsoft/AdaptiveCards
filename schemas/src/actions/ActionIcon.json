{
  "$schema": "https://raw.githubusercontent.com/microsoft/AdaptiveCards/6f39aedce45864ae1067ed44a5551dc973790bb5/source/nodejs/typed-schema/schema/lib/Type.json",
  "version": "1.3",
  "features": [
    3053
  ],
  "properties": {
    "url": {
      "type": "uri-reference",
      "description": "The URL to the image. Supports data URI.",
      "required": true
    },
    "tintable": {
      "type": "boolean",
      "description": "If `true`, the icon will be automatically themed to whatever color the host uses for action icons. Essentially, all non-transparent pixels in the image will be set to whatever color the host chooses."
    }
  }
}