{
	"$schema": "http://adaptivecards.io/schemas/adaptive-card.json",
	"type": "AdaptiveView",
	"version": "1.0",
	"body": [
		{
			"type": "Container",
			"items": "{{body}}"
		},
		{
			"type": "ActionSet",
			"actions": "{{actions}}"
		}
	]
}