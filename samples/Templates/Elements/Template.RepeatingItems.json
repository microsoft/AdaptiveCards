{ 
	"type": "AdaptiveCard", 
	"version": "1.2",
	"body": [ 
		{ 
			"type": "Container",
			"items": [ 
				{ 
					"type": "TextBlock", 
					"text": "List of Peers"
				},
				{ 
					"$data": "${employee.peers}",
					"type": "TextBlock", 
					"text": "${string($index + 1)}. ${name}"
				} 
			] 
		}
	]
}
