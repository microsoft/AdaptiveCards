{
	"name": "spec-generator",
	"version": "0.5.0",
	"description": "Generates the element/property tables for our specs based on the schema file.",
	"author": "AdaptiveCards",
	"license": "MIT",
	"main": "lib/spec-generator.js",
	"types": "lib/spec-generator.d.ts",
	"files": [
		"lib",
		"src"
	],
	"scripts": {
		"clean": "rimraf build lib dist",
		"build": "tsc",
		"dts": "dts-generator --name spec-generator --project . --out dist/spec-generator.d.ts",
		"lint": "tslint -c tslint.json 'src/**/*.{ts,tsx}'",
		"release": "npm run build && npm run dts",
    "build-all": "cd ../typed-schema-markdown && npm run build-all && cd ../spec-generator && npm run build"
	},
	"dependencies": {
    "marked-schema": "0.1.5",
    "typed-schema": "0.5.0",
    "typed-schema-markdown": "0.5.0",
		"p-iteration": "^1.1.8"
	},
	"devDependencies": {
		"rimraf": "^2.6.2",
		"typescript": "^3.2.2"
	}
}
