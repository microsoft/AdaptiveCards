{
	"name": "adaptivecards-data",
	"version": "0.1.0-alpha1",
	"description": "Adaptive Card data binding abd tmeplating engine for JavaScript",
	"author": "AdaptiveCards",
	"license": "MIT",
	"main": "lib/adaptivecards-data.js",
	"types": "lib/adaptivecards-data.d.ts",
	"files": [
		"lib",
		"dist",
		"src"
	],
	"scripts": {
		"clean": "rimraf build lib dist",
		"prebuild": "tsc",
		"build": "webpack",
		"watch": "webpack --watch",
		"start": "webpack-dev-server --open",
		"dts": "dts-generator --name adaptivecards-data --project . --out dist/adaptivecards-data.d.ts",
		"lint": "tslint -c tslint.json 'src/**/*.{ts,tsx}'",
		"release": "npm run build && webpack --mode=production && npm run dts"
	},
	"devDependencies": {
		"rimraf": "^2.6.2",
		"typescript": "^3.2.2",
		"webpack": "^4.27.1",
		"webpack-cli": "^3.1.2",
		"webpack-concat-plugin": "^3.0.0",
		"webpack-dev-server": "^3.1.14"
	}
}
