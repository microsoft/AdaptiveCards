{
  "name": "adaptivecards-controls",
  "version": "0.1.0",
  "description": "A library of pure JS/HTML controls designed for use with Adaptive Cards.",
  "main": "built/adaptivecards-controls.js",
  "private": true,
  "scripts": {
    "build-dist": "webpack",
    "tsc": "tsc -p .",
    "build": "npm run tsc && npm run build-dist && npm run dts",
    "watch": "webpack --watch",    
    "dts": "dts-generator --name ms-js-controls --project . --out dist/ms-js-controls.d.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "0.0.2",
    "@types/vkbeautify": "^0.99.0",
    "ajv-cli": "1.1.1",
    "dts-generator": "^2.1.0",
    "json-loader": "0.5.4",
    "ts-loader": "^2.0.0",
    "tslint": "^4.4.2",
    "typescript": "^2.1.5",
    "webpack": "^2.2.1"
  },
  "author": "Microsoft",
  "license": "MIT"
}