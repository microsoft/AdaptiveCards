<resources>

    <style name="AdaptiveCardsTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="adaptiveActionPositive">@color/colorAccent</item>
        <item name="adaptiveActionDestructive">@color/actionDestructive</item>
    </style>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="AdaptiveCardsTheme">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorPrimary</item>
        <item name="colorButtonNormal">@color/colorPrimary</item>
        <!--<item name="colorControlHighlight">@color/colorAccent</item>-->
        <item name="editTextBackground">@drawable/border</item>
        <item name="android:buttonStyle">@style/Widget.AppCompat.Button.Colored</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!--
    <style name="Adaptive.Action.Positive" parent="Widget.AppCompat.ActionButton">
        <item name="android:colorBackground">@color/colorAccent</item>
        <item name="android:textColor">@color/colorWhite</item>
    </style>
    -->
</resources>
